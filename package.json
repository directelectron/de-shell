{
  "name": "de-shell-dev",
  "version": "0.2.0",
  "private": true,
  "description": "Development-only: typechecks and unit-tests the TypeScript under de_shell/js. Nothing here is published \u2014 the TypeScript ships inside the de-shell wheel.",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "electron": "^34.0.0",
    "electron-updater": "^6.8.9",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.4.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:unit": "node --test \"de_shell/js/**/*.test.ts\" \"de_shell/js/**/*.test.cjs\""
  }
}
